// version.js — ЕДИНЫЙ источник номера версии клиента (выпуск v0.29, 25.09.2026). Грузится
// ПЕРВЫМ <script>: номер сборки нужен всем остальным файлам (syncPost шлёт его в каждом POST,
// logAction пишет в журнал, update.js сверяет с version.json и со штампами файлов).
// Правится ТОЛЬКО скриптом tools/bump-version.js — он же переписывает ?v= в index.html,
// <meta name="app-build">, текст .version, version.json, sw.js и штампы сборки в первой
// строке каждого своего файла. Ручная правка здесь = расхождение, которое поймает проверка
// согласованности (tools/bump-version.js --check) и «смешанная загрузка» в update.js.
// APP_BUILD — целое, СТРОГО растёт (формат ГГГГММДДNN); версии сравниваются ТОЛЬКО по нему
// (строки «0.29» и «0.3» так не сравнить).
const APP_VERSION='0.29';
const APP_BUILD=2026092502;
const APP_RELEASED='2026-09-25';
